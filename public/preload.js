// window.ipc = require('electron').ipcRenderer
// window.dialog = require('electorn').remote.dialog
// window.openExternal = require('electron').shell.openExternal

window.ipc = require('electron').ipcRenderer
window.dialog = require('electron').dialog
window.openExternal = require('electron').shell.openExternal
window.logger = require('electron-log')

